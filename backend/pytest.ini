[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: Tests d'intégration (avec GLPI réel)
    unit: Tests unitaires
    slow: Tests lents

# Ignorer warnings
filterwarnings =
    ignore::DeprecationWarning